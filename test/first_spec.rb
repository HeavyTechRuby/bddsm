BDDSM.describe 'Test case' do
  it { expect(1).to eq 1 }
  it { expect(1).to eq 2 }
  it { expect(1).to eq 1 }
  it { expect(1).to eq 1 }
  it { expect(1).to eq 1 }
  it { expect(1).to eq 1 }
  it { expect(1).to eq 7 }
end
